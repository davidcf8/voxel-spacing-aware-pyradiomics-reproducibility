# Public Release Audit

Audit date: 2026-09-17

## Scope

This audit was performed only in the clean working copy:

```text
PUBLIC_TECHNICAL_PAPER_REPRODUCIBILITY_CLEAN/
```

The source repository was not modified.

## Removed Material

Removed manuscript and drafting material from the clean copy:

- `manuscript/`
- `experiments/manuscript_insert_additional_validation.tex`
- `experiments/manuscript_insert_additional_validation_summary.md`
- `experiments/table_additional_validation_summary.tex`
- LaTeX report/table fragments under `experiments/synthetic_spacing_validation/`

Removed local generated artifacts from the clean copy:

- `.repro_build/`
- `.pytest_cache/`
- `__pycache__/`
- `tests/__pycache__/`
- `reproduced_results/`
- `results/rerun/`
- `results/reproduction_environment.json`

Scientific figure PDFs under `experiments/` were retained because they are reproducible experiment outputs, not manuscript source.

## Documentation Changes

Rewritten or revised:

- `README.md`
- `CITATION.cff`
- `AUDIT/README_AUDIT.md`
- `experiments/additional_validation_report.md`
- `experiments/synthetic_spacing_validation/README.md`

The README now distinguishes lightweight verification from full end-to-end reproduction and avoids unsupported claims of IBSI compliance, native-topology preservation, physical correctness, or clinical superiority.

`CITATION.cff` was parsed successfully as YAML and contains six authors. No DOI, ORCID, email, or repository URL was invented.

## File-Type Checks

- `.tex` files remaining: 0
- LaTeX auxiliary files remaining: 0
- `manuscript/` directory present: no
- Manuscript PDFs remaining: no
- Scientific experiment PDFs remaining: yes, under `experiments/*/figures/` and synthetic-validation output locations

## Local Paths, Privacy, and Secrets

Scanned versionable repository content for machine-local path patterns, sensitive-value patterns, legacy experimental branch names, and unsupported overclaiming phrases.

Result: no matches in versionable public content after excluding `.git` and binary scientific outputs. No sensitive values were detected.

## Git Ignore Status

`.gitignore` now includes:

- `manuscript/`
- `*.tex`, `*.aux`, `*.log`, `*.out`, `*.toc`, `*.bbl`, `*.blg`, `*.synctex.gz`
- `.repro_build/`, `.repro_env/`, `.repro_pyradiomics/`
- `.pytest_cache/`, `__pycache__/`, `tests/__pycache__/`, `*.pyc`
- `reproduced_results/`, `results/rerun/`, `results/reproduction_environment.json`

No disposable local build or rerun artifacts remain in the clean copy after validation cleanup.

## Validation Commands

Executed:

```bash
python reproduce_paper.py
```

Result:

```text
Overall status: PASS
```

Executed:

```bash
python rerun_experiments.py
```

Result:

```text
End-to-end rerun: PASS
Published-result verification: PASS
Overall status: PASS
```

Executed:

```bash
PYTHONPATH=/private/tmp/pyradiomics_release_pytest_shims:./.repro_build/pyradiomics_paper_overlay \
<PYTHON_3_10_ENV>/bin/python \
-X faulthandler -m pytest -q tests/test_voxel_spacing_aware_synthetic.py
```

Result:

```text
2 passed
```

The first pytest attempt without the temporary readline shim segfaulted during pytest startup in the known macOS/libedit readline path, before test collection. The shim was external to the repository and did not modify scientific source files.

Executed:

```bash
python rerun_experiments.py --skip-build
```

Result:

```text
End-to-end rerun: PASS
Published-result verification: PASS
Overall status: PASS
```

Finally executed:

```bash
python rerun_experiments.py --clean
```

and removed remaining disposable verification outputs.

## Reproduction Environment Observed During Validation

The end-to-end workflow reconstructed upstream PyRadiomics at:

```text
8ed579383b44806651c463d5e691f3b2b57522ab
```

During validation, the local build verification resolved:

- modified `radiomics` package path under `.repro_build/pyradiomics_paper_overlay/radiomics/__init__.py`
- modified `_cmatrices` binary under `.repro_build/pyradiomics_paper_overlay/radiomics/_cmatrices.cpython-310-darwin.so`

These generated paths were removed after validation cleanup and are intentionally regenerated by `rerun_experiments.py`.

## Remaining Concerns

No release-blocking issues were identified.

Add a Zenodo DOI to `CITATION.cff` after archival of the `v1.0.0-paper` release.
