# Audit

This clean repository was prepared locally from the technical-paper project experiment outputs. Original project files were not modified.

Key cleanup decisions:

- Used `/experiments` synthetic/phantom outputs only.
- Excluded clinical CT/MRI cohort material.
- Excluded virtual environments, build folders, caches, logs and compiled binaries.
- Excluded unrelated experimental preprocessing branches from the clean implementation snapshot and public parameter examples.
- Public parameter examples retain only NR, RS and VS settings, with unrelated preprocessing-branch lines removed.
